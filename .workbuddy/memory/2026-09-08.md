# 2026-09-08

- 评审了 kian-notes 知识库结构（57 篇 md）。给出建议：README/structure.txt 与实际目录不一致；命名混杂（未命名.md、test.md、中英混用）；English/cs_abbreviation.md 应归入 cs/；空目录（cs/network）；嵌套过深（os/linux/env_setup/linux/）。建议新增 _inbox、attachments、templates、archive 目录，用 frontmatter + tags + MOC 索引页支撑长期可用性。用户目标是搭建长久可用的知识库。

- 已决断：图片资源采用"同目录资源文件夹"方案（笔记 `X.md` 对应 `X.assets/`，相对路径引用），不用集中式 attachments/。理由：图片复用率低、便于整体搬迁。要求命名规则化、禁止绝对路径。待办：把现有分散 assets（Python Env Setup_resource、assets/Startup Disk/ 等）迁移到对应 `X.assets/`。
- 用户环境确认：Obsidian + Custom Attachment Location 插件（配置 `./assets/${noteFileName}`，旧迁移文件导致命名不一致）。已确认 Obsidian 内置"子文件夹"选项不支持 ${filename} 变量，插件必须保留。资源规则最终定型：目录内 `assets/<笔记名>/` 存图、相对路径引用、插件负责落位与改名同步；关闭内置附件子文件夹选项避免冲突。
- 框架搭建完成（已执行）：新增 `cs/algorithm/`（移入 Diffie-Hellman密钥交换.md，原 cs/algorithm.md）、`cs/network/`（一级主题，用户是数通工程师，待填资料）、`_inbox/`（收入 fragments.md、test.md、未命名.md）、`cs/docs/template/笔记模板.md`；压平 os/linux/env_setup 双层嵌套；English/ 保留。写 README.md（总索引）+ cs/docs/结构规范.md（index 阈值 >12 篇、归档手动且在子集内 _archive/、资源规则、命名规范）。未做资源迁移和归档。全程无删除文档，md 数 57→59，png 7→7。structure.txt 为二进制旧文件未动。

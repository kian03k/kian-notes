# conda
```bash
# 查看环境
conda env list

# 激活环境
conda activate web-dev

# 退出环境
conda deactivate

# 删除不用的环境
conda remove -n 环境名 --all

# 导出环境（备份）
conda env export > env.yml

# 从文件重建
conda env create -f env.yml
```